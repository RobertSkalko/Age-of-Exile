package com.robertx22.mine_and_slash.event_hooks.player;

/*
public class RightClickItemCastSpell implements UseItemCallback {

    @Override
    public TypedActionResult<ItemStack> interact(PlayerEntity player, World world, Hand hand) {

        /*
        ItemStack stack = player.getStackInHand(hand);
        GearItemData gear = Gear.Load(stack);

        if (gear != null && gear.GetBaseGearType()
            .isMageWeapon() || gear.GetBaseGearType()
            .isMeleeWeapon()) {

            Packets.sendToServer(new CastSpellPacket());

            return TypedActionResult.consume(stack);
        } else {
            return TypedActionResult.pass(stack);
        }


         */


